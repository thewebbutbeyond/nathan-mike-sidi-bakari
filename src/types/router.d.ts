// Pulls in @tanstack/start-client-core's module augmentation for FilebaseRouteOptionsInterface,
// which adds the `server.handlers` property to createFileRoute() options.
import "@tanstack/react-start";
