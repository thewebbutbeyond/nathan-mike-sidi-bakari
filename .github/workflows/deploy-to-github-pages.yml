name: Deploy to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment not configured yet
        run: |
          echo "Main currently stores planning docs and Lovable design direction only."
          echo "When a prototype branch becomes the release candidate, replace this placeholder with that app's build/deploy steps."
